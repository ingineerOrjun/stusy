{
  "name": "rgsc-study-board",
  "version": "1.0.0",
  "private": true,
  "description": "Bilingual (English + Nepali) interactive learning platform for Nepal's CDC Computer Engineering curriculum, Grades 9-12.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node _source/build/index.js",
    "test": "node --test --test-concurrency=1 tests/*.test.js",
    "validate": "node _source/build/index.js",
    "check": "npm run build && npm test",
    "serve": "node tests/helpers/serve.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
